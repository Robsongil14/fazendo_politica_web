{
  "scripts": {
    "dev": "npm run dev --prefix web_version",
    "build": "npm ci --prefix web_version && npm run build --prefix web_version",
    "start": "npm run start --prefix web_version"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1"
  },
  "devDependencies": {
    "@types/file-saver": "^2.0.7"
  }
}
